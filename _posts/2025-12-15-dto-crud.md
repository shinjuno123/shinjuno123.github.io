---
title: "DTO & CRUD endpoints"
date: 2025-12-15 00:00:00 +0800
categories: [asp.net]
tags: [asp.net]
---


# What is DTO?
- It is abbreviation of Data Type Object.
- DTO can be considered a contract between the server and the client.
- It represents how to data is transfered and used.

## Why are we using Record for DTO?

```cs
public record class GameDto {}
```

- By Default, Record is immutable. Once properties are set and the record is created, they can't be changed
- This immutability is perfect fit for DTO. Because the data is carried from one point to another without any modifications.
- Record reduces the boilerplate code that is typically assiciated with class definitions intended for data holding as DTOs.


## Why did I do CreatedAtRoute to return after saving the data?
- It includes the Location header in the HTTP response that provides a URI where the newly created resource can be retrieved with a GET request
- Return the created object itself in the response body.

## Example: How to test endpoints on .http file

```

### List all games
GET http://localhost:5115/games


### Invoke a specific game by ID
GET http://localhost:5115/games/2

### Create a new game
POST http://localhost:5115/games
Content-Type: application/json

{
    "name": "The Legend of Zelda: Breath of the Wild",
    "genre": "Action-adventure",
    "price": 59.99,
    "releaseDate": "2017-03-03"
}

### Update a specific game by ID
PUT http://localhost:5115/games/2
Content-Type: application/json

{
    "name": "Super Mario Bros.",
    "genre": "Platformer",
    "price": 39.99,
    "releaseDate": "1985-09-13"
}

### Delete a specific game by ID
DELETE  http://localhost:5115/games/2

```

### Example: GET, POST, PUT, DELETE


```csharp
using GameStore.Api;
using GameStore.Api.Dtos;

var builder = WebApplication.CreateBuilder(args);

var app = builder.Build();

const string GetGameEndpointName = "GetGame";

List<GameDto> games = [
    new GameDto(1, "The Witcher 3: Wild Hunt", "RPG", 39.99m, new DateOnly(2015, 5, 19)),
    new GameDto(2, "Cyberpunk 2077", "Action RPG", 59.99m, new DateOnly(2020, 12, 10)),
    new GameDto(3, "Minecraft", "Sandbox", 26.95m, new DateOnly(2011, 11, 18)),
    new GameDto(4, "Among Us", "Party", 4.99m, new DateOnly(2018, 6, 15)),
    new GameDto(5, "Hades", "Roguelike", 24.99m, new DateOnly(2020, 9, 17))
];

// GET /games
app.MapGet("games", () => games);

// GET /games/{id}
app.MapGet("games/{id:int}", (int id) =>
{
    GameDto? game = games.Find(game => game.Id == id);
    return game is null ? Results.NotFound() : Results.Ok(game);
}).WithName(GetGameEndpointName);

// POST /games
app.MapPost("games", (CreateGameDto createGameDto) =>
{
    GameDto game = new(
         games.Count + 1,
         createGameDto.Name,
         createGameDto.Genre,
         createGameDto.Price,
         createGameDto.ReleaseDate
    );

    games.Add(game);

    return Results.CreatedAtRoute(GetGameEndpointName, new { id = game.Id }, game);
});

// Put /games
app.MapPut("games/{id:int}", (int id, UpdateGameDto updatedGame) =>
{
    var index = games.FindIndex(game => game.Id == id);

    // There are 2 ways of handling this
    // 1. If the index is -1, we return a 404 Not Found response
    // 2. If the index is -1, we create a new game with the provided id and add it to the list
    // What to do here is not clearly defined in the requirements, so I'll go with option 1
    if (index == -1)
    {
        return Results.NotFound();
    }

    games[index] = new GameDto(
        id,
        updatedGame.Name,
        updatedGame.Genre,
        updatedGame.Price,
        updatedGame.ReleaseDate
    );

    return Results.NoContent();
});

app.MapDelete("games/{id:int}", (int id) =>
{
    games.RemoveAll(game => game.Id == id);
    return Results.NoContent();
});



app.MapGet("/", () => "Hello World!");

app.Run();


```

## Move endpoints to separate file
- github link: [using extension method](https://github.com/shinjuno123/asp-net-tutorial/tree/f081bb6f5f9fd7296d1aaedcaed7f4b2bae3dd15)